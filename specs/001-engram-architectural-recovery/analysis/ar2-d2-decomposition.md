# AR-2 D2 Decomposition — Project Identity V3 Expand

**Scope:** T016–T030 and applicable T088–T091 only
**Baseline:** `cfc0715f021998daea17d0fba56f5cf6191bbf9d`
**Integration branch/worktree:** `ar-2/identity-v3-expand` / `.agent/worktrees/ar-2-identity-v3-expand`
**Status:** execution graph for the AR-2 delta; implementation begins only after the corrected Spec Kit analysis and receipt authorize this exact candidate.

## Release invariant

Every V3-capable entry point submits one validated descriptor to the central `internal/projectidentity` workflow. Only that workflow resolves a server-issued UUID key. No transport, hook, daemon, cache, identifier, path, remote, name, legacy selector, or client input may choose a canonical key. Refusal happens before scoped access and causes zero scoped mutation.

## Shared cross-slice contracts

| ID | Producers | Consumers | Checkable invariants |
|---|---|---|---|
| `PI-V3-ANCHOR` | T017 | T018, T019, T022, T028 | Exactly `version`, `project_id`, `name`, `scope`; repository anchor is tracked directly beneath selected Git root; directory root is explicit; no upward inference; missing/legacy/malformed/copied/nested cases are typed refusals. |
| `PI-V3-DESCRIPTOR` | T016/T020 | T017–T019, T022, T024–T026, T028 | Same version 3 field names/normalization/reason codes in Go, JavaScript, and TypeScript; no canonical key, credentials, or raw private values; `client_instance_id` is explicit opaque non-secret configuration, never generated by resolution. |
| `PI-V3-OUTCOME` | T023 | T022, T024–T026, T027–T029 | Intent is one of `resolve_existing`, `register_anchor`, `read_filter`, `admin_target`; only registration writes a binding; success alone exposes UUID `project_key`; refusal exposes no key and does not mutate scoped/project/identifier/cache state. |
| `PI-V3-STORAGE` | T021 | T022, T027–T029 | Migration is additive/idempotent; existing `projects.id` and V2 fields remain readable; V3 key and anchor binding are nullable/unique; no backfill, merge, or legacy contraction; rollback preserves V3 evidence and restores V2 adapter reads. |
| `PI-V3-TRANSPORT` | T024–T026 | T027–T030 | Proto source plus both generated Go bindings stay in parity; every V3-capable HTTP/gRPC/MCP/daemon path invokes central resolution before scoped access; each maps the same typed result without raw DB/credential/path leakage. |
| `PI-V3-COMPARISON` | T027 | T028–T030 | Comparison is redacted/idempotent telemetry containing V3/V2 result, mismatch class, adapter/transport/scope/freshness/evidence fingerprint; mismatch never mints, merges, redirects, backfills, or reassigns a project. |
| `PI-V3-FIXTURE` | T028/T030 | T029/T090 | Isolated pgvector-capable PostgreSQL fixture has explicit accepted/refusal dual-representation cases, nonzero denominators, exact source/payload provenance, and rollback proof; synthetic AR-1 export alone is insufficient. |

## Dependency waves and exclusive ownership

### Wave 0 — reconcile and prepare

| Work | Owner | Allowed surfaces | Exit evidence |
|---|---|---|---|
| Current source reconciliation | Root | Spec Kit analysis artifacts only | `ar2-current-source-reconciliation.md` |
| Spec Kit path/traceability/checklist correction | Root documentation lane | `specs/001-engram-architectural-recovery/**` only | Corrected `tasks.md`, traceability, checklists, analysis report, receipt |
| AR-1 housekeeping | Dedicated housekeeping lane | Exact named worktrees/branches and root intake receipts only | Preservation manifest plus cleanup receipt; unrelated user file unchanged |

### Wave A — contract-first descriptor foundation

1. **T016:** contract maker owns `contracts/testdata/project_identity_v3_vectors.json` and only required contract clarifications. Vectors include accepted Git/SSH/SCP remote normalization, explicit non-default port/path-case cases, root/worktree/clone/move/nested/directory cases, malformed/credential/copy/client-key refusals, and fixed fixture `client_instance_id` values.
2. **T020:** test maker consumes the frozen vector file in `internal/projectidentity/project_identity_v3_vector_test.go` and `plugin/engram/hooks/project-identity-v3.test.js`; the RED observation occurs before production V3 implementation.
3. **T017:** anchor maker owns `internal/projectidentity/anchor.go` and direct parser/discovery tests plus the tracked root `.engram-project` migration. It does not create a server binding.
4. **T018 and T019 (parallel after anchor/vector freeze):** hook and OpenClaw makers each construct/validate descriptors and use explicit client instance configuration. Their file sets do not overlap.

**Wave A exit:** canonical vectors agree across Go/JS/TS; every unsafe anchor descriptor is refused before scoped access.

### Wave B — additive schema and central authority

1. **T021 and T023 (parallel):** migration maker owns GORM models/migration/store constraints and PostgreSQL behavior tests; outcome maker owns typed intent/result/error/audit-safe data in `internal/projectidentity`.
2. **T022 (serial):** resolver maker consumes the frozen anchor, storage, and outcome contracts. It owns resolution/registration workflow and persistence ports, including causally bound first mutation and zero-mutation refusals.

**Wave B exit:** real PostgreSQL migration/repeat apply and resolver tests pass; V2 rows remain readable; no historical row is merged/backfilled.

### Wave C — transport adoption

After T022/T023, run exactly three non-overlapping adapter makers in parallel:

| Task | Exclusive source set | Non-goal |
|---|---|---|
| T024 | `proto/engram/v1/engram.proto`, generated `engram.pb.go`/`engram_grpc.pb.go`, `Makefile`, `internal/grpcserver/**` V3 translation | Hand-editing generated files or defining resolver policy. |
| T025 | `internal/worker/handlers_context.go`, designated hook-intake route tests, HTTP result mapping | Calling V2 registration as a hidden V3 fallback or raw-project session-start bypass. |
| T026 | `internal/mcp/**`, `internal/handlers/engramcore/{slugcache,module,tools,grpcpool}.go`, `cmd/engram/wiring.go` | Giving `cmd/engram/main.go` identity ownership or creating a daemon resolver. |

All three retain an explicit V2 compatibility branch at the outer boundary and record comparison through T027's single comparison owner.

### Wave D — comparison, behavior matrix, receipt

1. **T027:** implement redacted idempotent comparison records.
2. **T028:** run the primary/worktree/clone/branch/move/remote/nested/non-Git/copied/malformed/ambiguous matrix using the real fixture.
3. **T029:** emit compatibility-window telemetry/receipt with nonzero supported-transport denominators, result classes, refusal count, capability state, and rollback boundary.

**Wave D exit:** one V3 key for accepted same-project contexts; zero scoped mutation for every unsafe case; V2 remains available; no comparison causes merge/backfill.

### Wave E — independent acceptance and closeout

1. **T030:** independent factual identity/vector and security checker plus locally staged dual-representation dogfood.
2. **T088–T091:** update traceability, bind independent checker/migration/security/transport receipts, run exact candidate test/payload/dogfood/rollback gates, and confirm no operator-surface implementation scope entered.
3. Root performs review synthesis and a fresh blind judge only after the frozen integrated candidate has clean focused gates. Findings return to the bounded owning maker lane; no checker edits product source.

## Worktree and integration policy

- The root integration worktree is the only branch that receives accepted commits.
- At most four maker worktrees may coexist with it; each maker gets an exclusive file set and writes no `.agent` artifact in its inner worktree.
- Root-owned durable receipts, reports, and closeout files write to the repository root `.agent/intake` package.
- Root integrates exact maker commits; root does not write product source in a maker's lane.
- A preserved AR-1 historical line is not an AR-2 source dependency. It is tracked separately through the housekeeping receipt and cannot silently enter the candidate.

## Checker contracts

| Check | Independent question | Evidence |
|---|---|---|
| Vector/factual | Do all three language implementations produce the exact descriptor/result contract? | Shared vector output, rejection reasons, source/descriptor hashes. |
| Migration | Does real PostgreSQL apply/reapply the additive schema and preserve V2 reads/rollback boundary? | Fixture database/schema query/receipt, repeat-run results. |
| Security/privacy | Can hostile descriptor/anchor/transport input reach a scoped mutation, leak a key, credential, path, raw DB error, or candidate enumeration? | Refusal matrix and redacted response/audit evidence. |
| Transport | Does every V3-capable gRPC/HTTP/MCP/daemon path invoke the one resolver and preserve typed outcomes? | Entry-point tests, generated parity, context propagation evidence. |
| Blind judge | Does the exact integrated candidate satisfy the frozen AR-2 contract without scope expansion? | Fresh read-only verdict bound to candidate HEAD. |

## T020 V3 Test Seam — D1

**Rung justification:** this is a reversible, AR-2-local test boundary whose consumers are the T017 Go anchor maker and the T018/T019 adapter makers; a wrong shape blocks three test suites but does not create a new subsystem.

### Boundary contract

- Go `internal/projectidentity` exposes only `ParseAnchorV3`, `DiscoverAnchorV3`, `NormalizeGitRemoteV3`, and `BuildDescriptorV3` over JSON-compatible `AnchorV3`, `DescriptorV3`, and typed legacy-evidence values.
- Hook JavaScript and OpenClaw TypeScript expose descriptor-only counterparts: `parseProjectAnchorV3`, `discoverProjectAnchorV3`, `normalizeGitRemoteV3`, and `buildProjectIdentityV3`.
- `ParseAnchorV3` rejects unknown/malformed fields; `DiscoverAnchorV3` receives an explicit repository or directory root and never searches upward; `NormalizeGitRemoteV3` returns a typed `normalized`, `omitted`, or `refused` disposition so local/file/malformed inputs are omitted while credential-bearing inputs are refused.
- `BuildDescriptorV3` requires an explicit non-empty opaque `client_instance_id`, rejects a client `project_key` assertion, and never generates or persists either value. The seam does **not** resolve a canonical key, select an intent, establish a binding, mutate state, access a database/cache, or replace any V2 path.

For T020/T017, the Go seam has these exact test-facing signatures: `ParseAnchorV3(raw []byte) (AnchorV3, error)`, `DiscoverAnchorV3(root, scope string) (AnchorV3, error)`, `NormalizeGitRemoteV3(raw string) (normalized string, disposition RemoteDisposition, err error)`, and `BuildDescriptorV3(anchor AnchorV3, remotes []string, legacy []LegacyIdentifierV3, clientInstanceID string) (DescriptorV3, error)`. JavaScript and TypeScript use equivalent JSON-object arguments/results and typed/throwing validation refusals. This is a test seam, not a public transport schema.

### Integration points

- T017 implements the Go seam and repository/directory discovery; its discovery input is an explicit selected root, not a current-directory or parent-anchor inference.
- T018 implements the JavaScript seam in hook identity construction; T019 implements the TypeScript seam in OpenClaw identity construction.
- T022 owns resolver intent/outcome, canonical key exposure, registration, and mutation causality; T020 must not pre-implement those assertions.

### Test plan

- T020 loads `contracts/testdata/project_identity_v3_vectors.json` directly in Go, JavaScript, and TypeScript.
- Its RED checks cover anchor field validation, remote normalization/disposition, descriptor field parity, explicit client instance IDs, and local refusal/no-leak behavior.
- T022/T028 consume the same vectors for resolution outcome, binding, canonical-key exposure, and zero-mutation assertions.

### Challenge-LITE

**REVISE applied:** the original broad harness would have invented resolver APIs. The corrected seam is descriptor-only, includes the explicit discovery/disposition boundaries, and keeps resolver outcomes in T022/T028.

- Keep the four helper families (selected): parse, explicit discovery, normalization with disposition, and construction separate validation, filesystem selection, and adapter parity without a wrapper/factory.
- Collapse parse/discovery into one loader: rejected because it obscures pure validation versus filesystem discovery and weakens T017/T020 boundaries.
- Use a neutral fixture runner: rejected because it adds a harness/dependency and hides per-language API parity.

### Checker commitment

One independent vector checker reruns the three focused T020 harnesses after T017/T018/T019 implementations exist and verifies that the shared corpus—not test-local rule copies—drives all three outcomes.

## Replan rule

A returned maker contradiction changes only the affected unstarted wave: correct the owning Spec Kit task/checklist/analysis, update this dependency graph, rerun its deterministic validation, then re-dispatch the smallest affected lane. It never authorizes AR-3 merge/backfill/cutover, UI work, production mutation, or an unbounded rewrite.