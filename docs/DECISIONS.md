# Architectural Decisions Index

One row per load-bearing decision. This index is the tracked, public anti-resurrection
record: detailed rationale may live in `.agent/specs/**` (private agent-state repo), but
the verdict line here is authoritative. Rule: any change that supersedes, permanently
removes, or declares "do NOT reopen" gets a row in the same PR.

Statuses: **active** (in force), **superseded** (points at successor).

| # | Decision | Verdict | Date | Status | Detailed record |
|---|----------|---------|------|--------|-----------------|
| 1 | v5 demolition — no resurrection | Graph stage, cross-encoder rerank, `internal/search` scoring passes, SDK observation extraction, and server-side MCP HTTP transports are removed. Leftover names in docs/models/env-templates are tombstones, not contracts. Classify any scaffold live/stale/dormant/must-build before building on it. | 2026-05 | active | `AGENTS.md` V5 DEMOLITION GUARD; `docs/arch/GOTCHAS.md` |
| 2 | stdio daemon + gRPC is the permanent MCP transport | Server-side HTTP MCP transports MUST NOT be reintroduced. MCP tools reach the server only via the `engram` stdio client proxy + gRPC on :37777 (cmux). | 2026-05 | active | `AGENTS.md`; `docs/arch/OVERVIEW.md`; `docs/arch/API_CONTRACTS.md` |
| 3 | Embedding dimension unified on 1536 | Both memory and code-chunk stores use `vector(1536)`. The archived vnext specs demanding 4096 are historical; do NOT reopen. Deployed and verified on prod in v6.14.0 (OQ-5). | 2026-06 | active | migration 142; agent-state: `specs/engram-absorption/adr/ADR-001-ci-a-topology.md` |
| 4 | Code intelligence ships flag-dark | SocratiCode-replacement tools (`codebase_index`/`codebase_status`/`codebase_search`) exist behind `ENGRAM_CODE_INTEL_ENABLED`; flag-off keeps `tools/list` byte-identical to the pre-v6.13.0 surface. | 2026-06 | active | `AGENTS.md` CODE INTELLIGENCE; agent-state: `specs/engram-absorption/adr/ADR-001-ci-a-topology.md` |
| 5 | Graph is explore-first; repair is secondary | The Graph surface is a read-first relationship explorer fed by verified automatic producers. Manual node/edge mutation lives only behind an explicit expert-repair mode with validation, audit, and readback. Supersedes the product reading of operator-console spec clarification C2 (raw CRUD forms as the primary workflow). | 2026-07-14 | active | agent-state: `specs/webui-docs-convergence-marathon/graph-product-contract.md` |
| 6 | Design promotion is one-way | `.od/` is the private authoring workspace; `design/operator-console/` is the tracked contract snapshot; `apps/operator-console/` is the deployable runtime. Promotion copies a curated allowlist one way; no generator, no bidirectional sync. Promotion refuses content changes without a `design_version` bump (G1-R3). | 2026-06 → hardened 2026-07-15 | active | `design/operator-console/PROMOTION-CONTRACT.md` |
| 7 | MCP tool surface carries no tombstones or alias ballast | A tool that only returns a "removed in v5" error, or that merely aliases a consolidated tool (`store_memory`, `vault_*`, `doc_*`, …), is removed from `tools/list` rather than kept for backward compatibility. Pre-production, single-operator: compatibility shims are ballast. | 2026-07-15 | active | agent-state: `specs/mcp-surface-1-obsolete-tool-prune.md` |
| 8 | muxcore/mcp-mux client embedding | The `engram` client embeds muxcore for shared-daemon multiplexing; Engram keeps a persistent always-connected owner policy while mcp-mux#140 (reusable dormant-supervisor contract) is open. Adopted through v6.42.x hotfix line (muxcore v0.27.1). | 2026-06 → v6.42.1 2026-07-14 | active | `docs/modules/README.md`; CHANGELOG v6.42.x |
| 9 | Rules live in a governed lifecycle, not free-form text | Behavioral rules get a domain contract with lifecycle tables and state-machine validation (RG-0), a background arbiter (RG-1), selective injection router behind `ENGRAM_RULE_ROUTER_ENABLED` (RG-2), and operator telemetry (RG-3, v6.29.0). | 2026-06 | active | agent-state: `specs/engram-rule-governance-telemetry/` + archived RG specs |
| 10 | Memory product layer frozen after CR-011 | ENG-MPL-1 shipped through v6.36.0 (selective temporal truth) and is frozen; new memory-product work opens a new lane instead of reopening MPL CRs. | 2026-07-07 | active | agent-state: `specs/memory-product-layer/` |
| 11 | v7 plug architecture is the canonical cognitive-layer program | Slices S1-S6 (state writer, meta-memory, ambient, directives capture, telemetry, outcome policy) shipped v6.37.0-v6.42.0. S4B directives-surfacing remains the only open slice. Predecessor `engram-v7-cognitive` is superseded. | 2026-07-04..06 | active | agent-state: `specs/engram-v7-plug-architecture/prd.md` |
| 12 | MVP control-plane cut | Operator console MVP deliberately cut model control-plane, vault rotation, and provider editing out of the first shipping scope. | 2026-06-20 | active | agent-state: `reports/2026-06-20-mvp-control-plane-cut.md` |
| 13 | `/health` embedded route belongs to machine health | On the embedded server the `/health` route returns machine-health JSON (registered before the SPA catch-all); the console health page is reachable via standalone Nuxt. Docs must not teach the embedded route as a page until P0 repairs it. | 2026-07-14 | active | `docs/arch/current-surface.json`; `internal/worker/static_health_route_collision_test.go` |
| 14 | Route truth uses a six-axis ledger | Console route claims separate `page_source`, `deployment_forms`, per-form `direct_load_status`, `capability_status`, `journey_status`, and auth/limitations/evidence. A page file alone never sets `journey_status=accepted`. | 2026-07-15 | active | `docs/arch/current-surface.json` (DOC-R3, commit f24113b8) |
| 15 | Write-dedup ships opt-in, default OFF | Effectiveness rank-9 write-dedup is gated off by default; enabling it is an operator decision, not a release default. | 2026-06 | active | agent-state: `specs/engram-effectiveness/rank9-write-dedup-scope.md` |
| 16 | Durable agent state lives in a private nested repo | `.agent/` stays gitignored in the public repo but is itself a git repo pushed to the private `thebtf/agent-state` remote (branch per project). Transient state (worktrees, tmp, scratch, evidence browser profiles, telemetry) stays untracked everywhere. | 2026-07-15 | active | `.agent/.gitignore`; github.com/thebtf/agent-state (private) |
| 17 | Planned releases in a legitimate run need no operator approval | A release inside a legitimate run (spec/PRD/ADR exists, run testing protocols executed) ships automatically when there is CONSUMER-PERSPECTIVE evidence the surface works — browser replay for UI, real stdio smoke for MCP tools, real CLI invocation for CLIs. Green unit tests alone are NOT sufficient; the release criterion is UXDD operability, not passing suites. Hard stops remain: destructive/irreversible, secrets, MAJOR/breaking public surface without policy, ambiguous version. | 2026-07-15 | active | operator directive 2026-07-15; agent-state memory `release-gate-uxdd-not-operator-approval` |
