# Claude Mnemonic Plus — Environment Configuration
# Copy to .env and edit: cp .env.example .env

# PostgreSQL
POSTGRES_PASSWORD=change-me-in-production
POSTGRES_PORT=5432

# Server ports
WORKER_PORT=37777
MCP_SSE_PORT=37778

# Authentication (empty = no auth; set for production)
API_TOKEN=

# Embedding provider: "openai" (LiteLLM/OpenAI-compatible) or "builtin" (ONNX BGE, 384 dims)
EMBEDDING_PROVIDER=openai
EMBEDDING_BASE_URL=http://localhost:4000/v1
EMBEDDING_API_KEY=
EMBEDDING_MODEL_NAME=openai/Qwen/Qwen3-Embedding-8B
EMBEDDING_DIMENSIONS=4096
