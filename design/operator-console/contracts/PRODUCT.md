# PRODUCT.md — engram Operator Console (pointer)

Product intent for the operator console lives in the root **`PRODUCT.md`** of the
engram repository. That file is the single authoritative home for console product
intent; read it instead of this one.

The mockup-era contract text that previously lived here (2026-06-18 direction,
design-system token summary, surface list) is **historical** — it described the
self-contained `.od/index.html` prototype phase and is superseded by the root
`PRODUCT.md`. For the committed design system and honesty contract, see
`DESIGN.md` and `DESIGN-SYNC-PROTOCOL.md` in this directory.
